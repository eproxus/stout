{erl_opts, [{parse_transform, lager_transform}]}.

{deps, [
    {lager, "3.2.4"},
    {color, "1.0.0", {pkg, erlang_color}}
]}.

{shell, [
    {apps, [stout]},
    {config, "config/shell.config"}
]}.
